version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Static site — no build step required"
    build:
      commands:
        - echo "Nothing to compile"
  artifacts:
    baseDirectory: /
    files:
      - "**/*"
  cache:
    paths: []
